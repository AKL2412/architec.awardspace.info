

{% extends "IntranetEtudiantBundle::layout.html.twig" %}
{%block title%} {{rubrique}}
	{%if data|length != 0 %} ({{data|length}}) {%endif%}
-{{etudiant.prenom}} {{etudiant.nom}} {%endblock%}
{%block body%}

	<div class=" bloc-message">
		<div class="row">
			
			<div class="col-md-2 menu">
				
                	<ul >
                		
                		<li class="ul">
                			<div class="btn-group">
							  <a href="{{path('intranet_etudiant_message',{rubrique:'message-contact'})}}" class="btn btn-danger btn-xs">
		                		Nouveau message
							  </a>
							  <button type="button" class="btn btn-danger btn-xs dropdown-toggle" data-toggle="dropdown">
							    <span class="caret"></span>
							    <span class="sr-only">Toggle Dropdown</span>
							  </button>
							  <ul class="dropdown-menu" role="menu">
							    <li>
								    <a href="{{path('intranet_etudiant_message',{rubrique:'message-administrateur'})}}">
								    Pour l'administrateur
								    </a>
							    </li>
							   <li>
								   <a href="{{path('intranet_etudiant_message',{rubrique:'message-adminstration'})}}">Pour l'administration
								   </a>
							   </li>
							    <li class="divider"></li>
							    <li><a href="#">Autre chose</a></li>
							  </ul>
							</div>
                		</li>

	                    <li class="ul {%if rubrique == 'boite-reception'%}
	                    	active {%endif%}"
	                    >
	                        <a href="{{path('intranet_etudiant_message',{rubrique:'boite-reception'})}}">
	                        	Boîte de réception 
	                        </a>
	                    </li>
	                    <li class="ul {%if rubrique == 'message-envoyes'%} active {%endif%}"
	                    >
	                        <a href="{{path('intranet_etudiant_message',{rubrique:'message-envoyes'})}}">
	                        	Messages envoyés
	                        </a>
	                    </li>
	                    <li class="ul {%if rubrique == 'contact'%} active{%endif%}"
	                    >
	                        <a href="{{path('intranet_etudiant_message',{rubrique:'contact'})}}">
	                        	Mes contacts
	                        </a>
	                    </li>
                    </ul>
                
			</div>
			<div class="col-md-9 contenu">
				{%if rubrique == 'message-envoyes'%}
	                    	messages envoyés
	            
	            {%elseif rubrique == 'contact'%}
	                    	mes contacts
	            
	            {%elseif rubrique == 'boite-reception'%}
	                    	ma boite de reception 
	            {%elseif rubrique == 'message-adminstration'%}
	                    	message-adminstration
	            {%elseif rubrique == 'message-administrateur'%}
	                   message-adminstrateur
	             {%elseif rubrique == 'message-contact'%}
	                   message à un contact
	            {%endif%}
				
			</div>
		</div>
	</div>
{%endblock%}



